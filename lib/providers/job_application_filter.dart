import 'package:flutter/material.dart';

enum ApplicationFilter { all, bookmark, apply, interview, pendingResponse }

class FilterColor {
  static const all = Color(0xFF2C2C2C);
  static const interview = Color(0xFFB7950B);
  static const pendingResponse = Color(0xFFD68910);
  static const apply = Color(0xFF0D47A1);
  static const bookmark = Color(0xFF5C6BC0);
}

